@using BackOffice.Translates

<ul id="tabs" class="nav nav-tabs">
    <li id="merchants"><a data-toggle="tab" onclick="selectTab(this.parentNode.id, '@Url.Action("MerchantsList")')">Merchants</a></li>
</ul>

<div id="MerchantsPage"></div>

<script>
    function selectTab(tabId, url) {
        if ($('#' + tabId).hasClass('active'))
            return;
        $('#tabs').children('li').each(function() {
            (this.id === tabId) && $('#' + this.id).addClass('active') || $('#' + this.id).removeClass('active');
        });
        requests.doRequest({ url: url, divResult: '#MerchantsPage' });
    }
    selectTab('merchants', '@Url.Action("MerchantsList")');
</script>